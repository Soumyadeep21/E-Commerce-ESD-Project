import 'package:flutter/material.dart';

class LocalStores extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Stores near you will be shown in this area !'),
    );
  }
}
